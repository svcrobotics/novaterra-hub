<header class="flex justify-between items-center p-6 bg-gray-100 shadow-md border-b border-gray-300">
  <div class="text-2xl font-bold text-gray-800">
    NovaTerra-Hub
  </div>
  <div class="space-x-4">
    <% if user_signed_in? %>
      <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "text-gray-600 hover:text-red-500 transition" %>
    <% else %>
      <%= link_to 'Connexion', new_user_session_path, class: "text-gray-600 hover:text-blue-500 transition" %>
      <%= link_to 'S\'inscrire', new_user_registration_path, class: "button-primary" %>
    <% end %>
  </div>

</header>
